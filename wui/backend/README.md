# WUI backend (optional)

CI and docs use **`docker/Dockerfile.backend`** at the repo root (tracked in git). If you add a `Dockerfile` here for a full WUI backend, add it under the same [`.gitignore`](../.gitignore) exceptions as `!wui/backend/Dockerfile`.
