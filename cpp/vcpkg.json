{
  "$schema": "https://raw.githubusercontent.com/microsoft/vcpkg-tool/main/docs/vcpkg.schema.json",
  "name": "qminiwasm-cpp",
  "version-string": "0.1.0",
  "description": "C++ deps for qminiwasm (gRPC training engine + delta_sync proto)",
  "dependencies": [
    "grpc"
  ],
  "features": {
    "training-libtorch": {
      "description": "LibTorch (CPU) for native TPEM training; heavy. Prefer CMAKE_PREFIX_PATH to a prebuilt LibTorch if you skip this feature.",
      "dependencies": [
        "libtorch"
      ]
    }
  }
}
