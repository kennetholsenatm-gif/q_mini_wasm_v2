# WUI backend API only (no torch/quantum/wasm)
# Pinned minimums for security (Trivy); bump transitives (e.g. starlette) if still flagged.
pydantic>=2.10.0
pydantic-settings>=2.6.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12