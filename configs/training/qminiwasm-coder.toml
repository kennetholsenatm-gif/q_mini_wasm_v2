# Generated by training-wui Build + Run
# Mode: learning-rate trial run (short budget)
# Requested data_source=hybrid_mesh_hf_tabular -> source=hf_tabular + mesh_blend_fraction>0

[hardware]
accelerator = "xpu"
quantum_backend = "penny_lane"
device_index = 0

[training]
epochs = 100
batch_size = 32
learning_rate = 0.0001
grad_clip_norm = 1.0

[checkpoint]
save_path = "artifacts/qminiwasm-coder.pt"
best_path = "artifacts/qminiwasm-coder_best.pt"
latest_path = "artifacts/qminiwasm-coder_latest.pt"

[data]
source = "hf_tabular"
path = "code-search-net/code_search_net"
mesh_algorithms = "hash,encrypt,network,routing,consensus"

[huggingface]
split = "train"
mesh_blend_fraction = 0.2

[eval]
holdout_fraction = 0.05
every_epoch = true

[cascade]
enabled = true
steps_per_epoch = 2
group_size = 4
